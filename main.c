#include <stdio.h>
#include <conio.h>
#include "my_mat.h"

int flag = 1;

while(flag == 1){
  char = getchar();
  switch (char){
    case 'A':
      get_matrix();
      break;

    case 'B':
      // statements
      break;

    case 'C':
      // statements
      break;

    case 'D':
      flag = 0;
  }
}
