#include <stdio.h>
#include "my_mat.h"

int main(void)
{
  int flag = 1;

  while (flag == 1)
  {
    char = getchar();
    switch (char)
    {
    case 'A':
      int mat[10][10] = get_matrix();
      break;

    case 'B':
      // statements
      break;

    case 'C':
      // statements
      break;

    case 'D':
      flag = 0;
    }
  }
  return 0;
}
